<%= form_for(@growl) do |f| %>
  <% if @growl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@growl.errors.count, "error") %> prohibited this growl from being saved:</h2>

      <ul>
      <% @growl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
